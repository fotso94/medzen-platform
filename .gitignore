.venv/
__pycache__/
*.pyc
.DS_Store
.DS_Store
mlflow.db
mlruns/
.cache/
artifacts/
tmp/

# Terraform working directories and local runtime state are generated locally.
# Keep the dependency lockfile, but never commit provider binaries or state.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Recoverable local Git history-migration bundles; never publish them in Git.
.git-history-backups/
