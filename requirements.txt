# Pinned 2026-07-29 against a verified-working environment (Python 3.13.3).
# pyarrow 25 + macOS/arm64: NEVER break out of a remote Parquet iterator
# (Apache Arrow #45214). The WaxalNLP adapter downloads shards and reads
# locally for exactly this reason.
datasets==5.0.1
pyarrow==25.0.0
huggingface-hub==1.25.1
hf-xet==1.5.2
fsspec==2026.6.0
aiohttp==3.14.3
filelock==3.32.0
numpy==2.4.6
soundfile==0.14.0
librosa==0.11.0
boto3==1.43.58
PyYAML==6.0.3
jsonschema==4.26.0
pytest==9.1.1
