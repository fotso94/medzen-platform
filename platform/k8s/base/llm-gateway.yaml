# GENERATED by platform/generate.py from services.yaml — DO NOT EDIT
apiVersion: v1
kind: ServiceAccount
metadata:
  name: llm-gateway
  namespace: medzen
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-gateway
  namespace: medzen
spec:
  replicas: 2
  selector:
    matchLabels:
      app: llm-gateway
  template:
    metadata:
      labels:
        app: llm-gateway
    spec:
      serviceAccountName: llm-gateway
      containers:
      - name: llm-gateway
        image: REPLACE_AT_B0.dkr.ecr.eu-central-1.amazonaws.com/medzen-llm-gateway:PLACEHOLDER_TAG
        ports:
        - containerPort: 8082
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: '1'
            memory: 1Gi
        envFrom:
        - configMapRef:
            name: llm-gateway-config
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8082
          periodSeconds: 30
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: medzen
spec:
  type: ClusterIP
  selector:
    app: llm-gateway
  ports:
  - port: 8082
    targetPort: 8082
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: llm-gateway
  namespace: medzen
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: llm-gateway
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
