# GENERATED by platform/generate.py from services.yaml — DO NOT EDIT
apiVersion: v1
kind: ServiceAccount
metadata:
  name: speech-orchestrator
  namespace: medzen
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: speech-orchestrator
  namespace: medzen
spec:
  replicas: 2
  selector:
    matchLabels:
      app: speech-orchestrator
  template:
    metadata:
      labels:
        app: speech-orchestrator
    spec:
      serviceAccountName: speech-orchestrator
      containers:
      - name: speech-orchestrator
        image: 558069890522.dkr.ecr.eu-central-1.amazonaws.com/medzen-orchestrator:PLACEHOLDER_TAG
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: '2'
            memory: 2Gi
        envFrom:
        - configMapRef:
            name: speech-orchestrator-config
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          periodSeconds: 30
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: speech-orchestrator
  namespace: medzen
spec:
  type: ClusterIP
  selector:
    app: speech-orchestrator
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: speech-orchestrator
  namespace: medzen
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: speech-orchestrator
  minReplicas: 2
  maxReplicas: 6
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
