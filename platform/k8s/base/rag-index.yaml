# GENERATED by platform/generate.py from services.yaml — DO NOT EDIT
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-index
  namespace: medzen
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-index
  namespace: medzen
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rag-index
  template:
    metadata:
      labels:
        app: rag-index
    spec:
      serviceAccountName: rag-index
      containers:
      - name: rag-index
        image: 558069890522.dkr.ecr.eu-central-1.amazonaws.com/medzen-rag-index:PLACEHOLDER_TAG
        ports:
        - containerPort: 8083
        resources:
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: '2'
            memory: 4Gi
        envFrom:
        - configMapRef:
            name: rag-index-config
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8083
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8083
          periodSeconds: 30
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: rag-index
  namespace: medzen
spec:
  type: ClusterIP
  selector:
    app: rag-index
  ports:
  - port: 8083
    targetPort: 8083
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rag-index
  namespace: medzen
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rag-index
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
