# GENERATED by platform/generate.py from services.yaml — DO NOT EDIT
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tts-gateway
  namespace: medzen
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tts-gateway
  namespace: medzen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tts-gateway
  template:
    metadata:
      labels:
        app: tts-gateway
    spec:
      serviceAccountName: tts-gateway
      containers:
      - name: tts-gateway
        image: 558069890522.dkr.ecr.eu-central-1.amazonaws.com/medzen-speech-tts-gateway:PLACEHOLDER_TAG
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: '1'
            memory: 1Gi
        envFrom:
        - configMapRef:
            name: tts-gateway-config
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          periodSeconds: 30
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: tts-gateway
  namespace: medzen
spec:
  type: ClusterIP
  selector:
    app: tts-gateway
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tts-gateway
  namespace: medzen
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tts-gateway
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
