# DATA-SCOPE language declaration (hand-authored on the data branch).
# Added 2026-08-11 for the owner-approved capped Common Voice rw ingest
# (~300 h train + ~10 h eval). NOT generated by scripts/generate_languages.py:
# kinyarwanda is not yet in registry/sources.yaml and carries no B4/B5 scope
# decision — training/serving activation would require the usual owner scope
# record. This file exists so the A3 manifest validator recognises the
# language for curated-data purposes only.
alias: kinyarwanda
iso_code: kin
display_name: Ikinyarwanda
status: data_only
capabilities:
  asr:
    available: true
    sources:
    - common_voice
asr:
  artifact: null
  approved_version: null
  decode_strategy: pending_experiment
