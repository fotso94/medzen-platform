# Local B6.1 runtime pins. The deployment image is intentionally deferred
# until the local contract and service exit is reviewed.
fastapi==0.141.1
starlette==1.3.1
uvicorn==0.52.0
# Bedrock Knowledge Base backend (RAG_BACKEND=bedrock)
boto3==1.43.65
